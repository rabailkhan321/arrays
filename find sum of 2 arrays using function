#include<stdio.h>
void   is_print(int a[]);
void is_sum(int a[], int b[], int c[]);
int main()
{
    int a[30], b[30], c[5];
    int i;
    printf("enter the elements of array 1");
    for(i=0; i<5 ; i++)
    {
        scanf("%d", &a[i]);
    }
    printf("enter elements of array 2");
      for(i=0; i<5 ; i++)
    {
        scanf("%d", &b[i]);
    }
    is_print(a);
    is_print(b);
    is_sum(a, b, c);
}
void   is_print(int a[])
{
    int i;
          for(i=0; i<5 ; i++)
          printf("the element of array  are %d\n", a[i]);
}

void is_sum(int a[], int b[], int c[])


{
    int i;
     for(i=0; i<5 ; i++)
     
     {
         c[i]=a[i]+b[i];
         
         printf("the third array element at index %d is %d\n", i, c[i]);
     }
}
