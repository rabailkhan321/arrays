#include<stdio.h>
int main()
{
    int i;
    int a[20];
    printf("enter the elements of the array");
    
  for(i=0; i<10; i++)
  {
  scanf("%d", &a[i]);
  }
  
  for(i=0; i<10; i++)
  {
  printf("the elemens of %d array is %d\n",i, a[i]);
  }
}
